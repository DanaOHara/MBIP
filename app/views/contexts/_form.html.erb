<%= form_with(model: context, local: true) do |form| %>
  <% if context.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(context.errors.count, "error") %> prohibited this context from being saved:</h2>

      <ul>
      <% context.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contextlevel %>
    <%= form.text_field :contextlevel %>
  </div>

  <div class="field">
    <%= form.label :instanceid %>
    <%= form.text_field :instanceid %>
  </div>

  <div class="field">
    <%= form.label :path %>
    <%= form.text_field :path %>
  </div>

  <div class="field">
    <%= form.label :depth %>
    <%= form.number_field :depth %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<?php
/*$fs = get_file_storage();

// Prepare file record object
$fileinfo = array(
    'component' => $argv[1],
    'filearea' => $argv[2],     // usually = table name
    'itemid' => $argv[3],               // usually = ID of row in table
    'contextid' => $argv[4] // ID of context
    'filepath' => '/',           // any path beginning and ending in /
    'filename' => $argv[5]); // any filename

// Get file
$file = $fs->get_file($fileinfo['contextid'], $fileinfo['component'], $fileinfo['filearea'],
        $fileinfo['itemid'], $fileinfo['filepath'], $fileinfo['filename']);*/


        

        ?>
